---
name: data-viz-studio
description: 把用户上传的数据集（CSV/Excel/JSON）+ 一个数据问题，做成「面向特定受众、用对图像语言」的可视化图表（自包含 HTML）。当用户上传表格数据、想把其中某个数据问题或某一列做成图传达给特定受众时使用——尤其当受众的统计基础参差、需要替他们挑图型、做降级翻译时。涉及「这张图给谁看 / 他看得懂吗 / 选什么图型 / 数据可视化 / 出图 / 作图 / 图表选型」时都应触发本 skill。
---

# Data Viz Studio

## 目的
面对合适的受众，用合适的图像语言传达数据洞察——以最低沟通成本、最低调试成本出图。

**核心只有一根轴：受众。** 受众决定图表「是什么」（图型 + 必需注释，结构层、不可逆、用户自己判断不了）。场合只决定图表「穿什么」（配色 / 字体 / 尺寸 / 格式，样式层、可逆、用户自己有主见），是出图后的可选精修。

## 触发
用户上传 Excel/CSV/JSON，想把其中某个数据问题做成图表传达时使用。

## 输入
1. 数据集文件（.csv / .xlsx / .xls / .json）
2. 待回答的数据问题（或要重点看哪一列）
3. 受众能读懂哪类图：箱线图 / 直方图 / 都吃力 / 不清楚（内部映射见「受众轴」）

注：**应用场合不是必填输入**。默认出「标准版」；用户拿到后若要按实际场合精修，再进第 4.1 步。

## 何时不适合
1. 实时数据流的可视化。
2. 理科期刊投稿图：读者恒为同行专家（用不上本 skill 的受众翻译引擎），且需要矢量 PDF/EPS、双栏窄尺寸（本 skill 出 HTML）——这类有专门工具，不在范围内。

## 工作流

### 第 0 步 · 用户上传数据集文件

### 第 1 步 · 确定这张图要回答的问题
没说清就主动问："你想用这张图回答什么问题？" 或 "想重点看哪一列？"

### 第 1.5 步 · 确定受众能读懂哪类图
如果用户没说明受众水平，用自然语言追问，**不要暴露** `low` / `mid` / `high` 或「基础型 / 进阶型 / 高阶型」这套内部档位：

> 这张图的受众大概能看懂哪类图？  
> A. 能直接看懂箱线图  
> B. 看不懂箱线图，但能看懂直方图  
> C. 直方图也需要明显解释  
> D. 我不清楚受众的统计学素养

内部映射：A → `high`；B → `mid`；C → `low`；D → `unknown`，渲染前归一化为 `low`。  
选择 D 时不要告诉用户“按基础型处理”或“已降级”，只按保守的低门槛图像语言出标准版。

### 第 2 步 · 进数据做 EDA，挖出洞察（insight）
这一步是**推理**，由 Claude / 当前模型做；产出一个 insight 字符串传给渲染层，不在渲染层里现挖。

### 第 3 步 · 按受众统计素养选型 → 出【标准版】
依「受众轴」选图型 / 决定是否降级，调 `scripts/dataviz.visualize()` 出图（**不传 occasion**）。
标准版 = `STANDARD_PALETTE` + `STANDARD_STYLE`（70 分视觉地板）+ 受众注释地板。这一步就把标准版交付给用户。

⛔ 红线:选型与出图只能走 visualize(),绝不自己写 matplotlib
图型选择、降级、注释地板,全部由 visualize() 内的 _route / _annotation_floor 统一决定(单一真源)。一旦自己手写 matplotlib 出图,这套全部失效——路由被绕过、注释地板不生效、整根受众轴形同虚设。
出错时也不许破例:若 visualize() 报错(字体、缺列、编码…),在渲染层内部定位修复,或把问题如实报告给用户;绝不用"我直接写 matplotlib 绕过去"来救场。中文字体问题,渲染层的三级守卫早已处理——不要重新发明它、更不要绕开它。

⛔ 红线:标准版不是草稿版,必须过 70 分视觉地板
不允许把 Matplotlib/Seaborn 默认外观直接交付。所有标准版图表必须由 `scripts/dataviz/config.py::STANDARD_STYLE` 和 `scripts/dataviz/charts.py` 的统一样式 helper 接管画布、字体层级、背景、边框、网格与图例。新增图型也必须调用统一样式 helper,不能在图型函数里散落自定义字号和默认布局。

### 第 4 步 · 交付与精修
- **4.0** 用户觉得好 → 导出，工作流结束。
- **4.1** 用户按实际场合提精修：主题色 / 字体 / 导出尺寸 / 注释密度 / 导出格式等。
  传 `occasion="keynote" / "internal" / "portfolio"` 套对应场合预设。
  **铁律：场合只能在受众注释地板之上叠加注释，绝不能减到地板以下**。

### 自检
对照下方自检列表过一遍，修掉明显问题再交付。

## 受众轴
分界线钉在具体图型上：**高阶型统计素养人群 / 进阶型统计素养人群 = 箱线图；进阶型统计素养人群 / 基础型统计素养人群 = 直方图（分布）**。

| 受众档 | 分界判据 | 选型动作:该出什么图 | 注释地板（固有属性） |
|--|--|--|--|
| **高阶型统计素养人群（`high`）** | 可直接读懂**箱线图** | 数据形状对应的完整图型(箱线/散点+回归/热力…),不翻译 | 主标题 + 轴标签 |
| **进阶型统计素养人群（`mid`）** | 对箱线图需要辅助解读，但能读懂**直方图** | 与 high 相同的完整图型 + 阅读指引(翻译,不降级) | 主标题 + 轴标签 + 阅读指引 ） |
| **基础型统计素养人群（`low`）** | 看不懂直方图 | 降级:分布类→均值+范围条、复杂图→大数字 KPI;配直接标注 | 主标题 + 轴标签 + 直接标注 + 一句结论 |
| **未知受众（`unknown`）** | 用户无法判断受众统计素养，常见于大型公共演讲 / 开放听众 | 内部归一化为 `low`，按保守低门槛图像语言出图；不要对用户显式说明降级 | 同 `low` |

⚠️ 「能读懂 X」≠「就画 X」。 mid 能读懂柱状图,但该给他的是箱线图 + 阅读指引。因为箱线图信息更全,而 mid 在帮助下可以理解。把图降级成柱状,是针对 low 才做的事。选型一律由 visualize() 的 _route 决定,不要自己按"受众能读懂什么"去挑图。
注释地板是**受众档的固有属性**，不是一个可调旋钮：每一档的图型与它必需的注释是一体的。
为什么用图型而非职业定义：职业（高管 / 科研）只是统计素养的代理，且常猜错。换成「能读懂什么图」，也正好能直接拿去问用户："你的受众能直接看懂箱线图吗？"

## 注释模型
- **地板层 = 受众设定**（结构，烤进标准版）。
- **叠加层 = 场合设定**（样式，4.1 才加）。
- **实现**：单一真源 `scripts/dataviz/routing.py::_annotation_floor(audience, chart_kind)`；临界图型集见 `scripts/dataviz/config.py::_BORDERLINE_FOR_MID`。
  `scripts/dataviz/config.py::normalize_audience_key(...)` 负责把 `unknown` / `unclear` / `unsure` 等不确定输入归一化为 `low`。

## 标准版视觉地板
目标是 70 分可交付质量：中性、干净、完整，不追求作品集/keynote 的强风格，但不能显得临时潦草。

所有标准版默认遵守：
- 画布约 16:10，默认由 `STANDARD_STYLE["figsize"]` 控制；标题、轴标签、注释不得挤压图表主体。
- 使用统一中文字体栈；标题、轴标签、刻度、注释、图例有明确字号层级。
- 背景为暖白或纯白，主文字深灰，次级文字灰，网格线浅灰，只用一个强调色；非重点数据用中性灰蓝。
- 去除上/右边框，弱化坐标轴，只保留必要的浅色网格线。
- 标题回答问题，轴标签使用可读名称；图例不遮挡数据，数字最多保留 1-2 位小数。
- 基础型受众的结论放在标题下或图表下方，图内只放必要的重点数值；标签、注释不得遮挡数据结构。

## 自检列表

### P0（对错）
1. **图表回答的是不是用户原话里那个问题？** 比较对象 / 分组 / 子总体 / 度量 / 口径必须与用户逐字对应；严禁把 A vs B 擅自换成别的。
2. **标准版有没有被悄悄套了某个场合的风格？** 第 3 步只该用标准样式基线 + 受众地板；场合风格只在用户进 4.1 后才加。
3. 中文是否妥善渲染？CSS / 字体里的 CJK font-family 是否加载到？
4. 图表类型是否适配数据类型？
5. 这张图是 visualize() 出的吗? 有没有自己手写 matplotlib、绕过路由和注释地板?绕过了 = 作废重来。

### P1（易读性）
1. 标签 / 数字有没有与任何图表结构（柱体、误差棒、网格线、坐标轴、相邻标签）重叠？重叠就移锚点或缩字号。
2. 标准版是否过 70 分视觉地板？检查画布比例、字体层级、暖白背景、浅网格、弱化坐标轴、图例位置、注释位置。

## 维护
<每次手动改了图，把那条规则回填到对应小节——这是经验沉淀的入口。
新增图型时，记得在 `scripts/dataviz/config.py::_BORDERLINE_FOR_MID` 登记它对进阶型统计素养人群是否临界。
新增 deck 槽位时，在 `references/deck.md` 的「槽位系统」表里登记它的点亮条件。>

## 详细参考
- 图型目录、路由表、`chart_kind` / `facet_col` 覆盖用法：`references/chart-catalog.md`
- 4.1 演示版式（Deck）外壳、槽位和 `render_deck(...)` 用法：`references/deck.md`
