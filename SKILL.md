---
name: data-viz-studio
description: 根据用户上传数据集和数据问题生成具备数据洞察的可视化图表（HTML 页面）
---

# Data Viz Studio

## 目的
根据数据集、数据问题、图表的应用场景、图表受众的技术画像，生成沟通成本低、调试成本低的数据可视化图表。

## 触发
当用户上传 Excel/CSV/JSON、想把其中某个数据问题做成图表传达时使用

## 何时使用 / 何时不适合
- 使用：
1. EXCEL/CSV/JSON 这三种格式的数据集文件
2. 高（科研/数据/分析）、中（业务/产品）、低（高管/跨职能/公众）三种不同统计学基础的受众
3. 期刊论文、大型公众演讲、团队内部汇报、作品集页面 四种可视化图表的应用场景

- 不适合：
1. 实时数据流的可视化生成

## 输入
1.可视化图表受众的数据素养
2.可视化图表的应用场景
3.数据集
4.待回答的数据问题

## 工作流

### 第 0 步 · 用户上传数据集文件

### 第 1 步 · 确定这张图表要传达的信息

如果用户没给清楚，主动问："你想要通过这个图表回答什么问题？" 或 “你想要重点关注这个数据集里哪一列的数据？”

### 第 2 步 · 根据数据问题进入数据集开始EDA，得到数据洞察

### 第 3 步 · 根据受众的数据素养，进行可视化图表选型

### 第 4 步 · 根据应用场景，选择配色方案以及注释信息颗粒度

### 第 5 步 · 调用渲染层出图
把前 4 步定好的参数传给 `data-viz_template.py` 的 `visualize()`（完整签名见下方「调用渲染层」），由它套配色、选图型/降级、定注释、出图，返回自包含 HTML。
关键映射：把数据问题落到 `group_col`（分类列）/ `value_col`（数值列），把洞察里要强调的那一组设为 `emphasize`。
这一步只渲染、不做判断——判断已在第 1–4 步完成；**不要**在这里现写 matplotlib 代码。

### 第 6 步 · 自检
对照自检列表的清单快速过一遍自己刚画的图，修掉明显问题再交付。



## 决策依据


### 受众轴 → 统计学基础越低，复杂编码越要“降级翻译”。
|画像|可直接读懂|降级规则|
|--|--|--|
|高（科研/数据/分析）|箱线、小提琴、ECDF、散点+回归、热力图、对数轴、误差棒|几乎不需解释|
|中（业务/产品）|条形、折线、分组/堆叠、直方、散点|箱线图临界 → 加一句解读；要清晰标签 + 结论句|
|低（高管/跨职能/公众）|条形、折线、大数字 KPI、简单部分对整体|分布类降级成"均值+范围条"或带标注条形；避免箱线/小提琴；重直接标注、一图一意|

### 场合轴 →
|场合|配色性格|注释密度|导出|
|--|--|--|--|
|理科期刊图|克制、色盲安全、可灰度打印|图上克制，细节靠图注|矢量高清 PDF/EPS，窄栏尺寸|
|大型公众演讲单页|一个高亮 + 中性灰，大色块|极少（讲者口头解释）|大字、投影远距可读|
|团队内部汇报|团队/品牌色|适度|效率优先|
|作品集/对外|自定义排印、强设计语言|引导式叙事标注|出版级、矢量|

### 注释组合规则 →
1. 注释 = 场合设基准，低画像再加一档

## 调用渲染层

```python
# ① 先把用户上传的文件读成 DataFrame（按扩展名分发，自动处理 CSV 的 GBK/UTF-8 编码）
df = load_dataframe(<上传文件路径>)        # 支持 .csv / .xlsx / .xls / .json

# ② 调渲染层出图
调用 data-viz_template.py 的 visualize(df, group_col, value_col,
    question=…, insight=<第2步EDA得到的洞察>,
    audience=<high/mid/low>, occasion=<journal/keynote/internal/portfolio>,
    emphasize=<要强调的组>,
    hue_col=<可选：要对比的子总体列，如 survived；给了就出"分组对比图">,
    emphasize_hue=<可选：高亮哪个子总体，如 "幸存">,
    save_to="output.html")
→ 返回一个自包含 HTML（含主图 + 候选视角）。这层只渲染，不做判断。
→ value 列若是 0/1 比例会自动走比率图；给了 hue_col 则走两/多子总体跨类别对比。
```

## 自检列表
### P0（对错）:
1. **图表回答的是不是用户原话里的那个问题？** 比较的对象 / 分组 / 子总体 / 度量 / 口径，必须和用户说的逐字对应。严禁把用户要的 A vs B 擅自换成别的（例：把"全体 vs 幸存"换成"幸存 vs 遇难"、把子集换成补集、把"分布"换成"均值"）；图例 / 注释 / 标题用词也要忠实用户的措辞。若用户的框架对当前图型不顺手（如两个子总体有重叠），**回去问用户、不要静默替换**。
2. 中文字是否已妥善渲染？网页字体 / CSS 里的 CJK font-family 有没有加载到？
3. 检查图表类型是否适配数据类型

### P1（易读性）:
1. 标签/数字有没有和任何图表结构重叠？（柱体、误差棒、网格线、坐标轴、相邻标签都算）——重叠就移动锚点或缩小字号。


## 维护
<每次手动改了图，把那条规则回填到哪一节——这是经验沉淀的入口。>
